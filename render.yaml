services:
  - type: web
    name: react-assignment-${RENDER_GIT_REPO_SLUG}
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    autoDeploy: true
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
